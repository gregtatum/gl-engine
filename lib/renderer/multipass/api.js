/*
import { multipass } from 'gl-engine'

let renderPass = RenderPass({ camera: camera })
let bloomPass = BloomPass()
let fxaaPass = FXAAPass()

let processor = multipass()
  .use(renderPass, bloomPass, fxaaPass)

//Or

let processor = multipass()
  .use(RenderPass({ scene, camera }))
  .use(FXAAPass())
  .use(BloomPass())
*/
